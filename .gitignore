# Credentials — never commit (use ibmiscrt.json.template as a starting point)
ibmiscrt.json

# Downloaded spool file output
QP1ARCY_*.txt
QP1ARCY_*.pdf

# Temp files from the Bash script
/tmp/ibmi_splf_list_*.json
