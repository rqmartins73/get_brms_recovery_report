# Credentials — never commit (use ibmiscrt.json.template as a starting point)
ibmiscrt.json

# Downloaded spool file output
QP1ARCY_*.txt
QP1ARCY_*.pdf

# IFS temp files land on the IBM i and are deleted automatically; nothing local to ignore
